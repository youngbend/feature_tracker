To install and run:

```
cmake .
make
./bin/test
```

To add your own main file, create main in project dir and add project description to
CMakeLists.txt. Then, run:
```
cmake .
make
./bin/{project_name}
```
